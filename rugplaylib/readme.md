this thing can be used if you are tpo lazy to get the lib yourself
NOTES: for components remove the components from the url and for components/ui remove components/ui
the reason for this is that it was too big for github to get all at once and i broke it a bit so all of the components/ui and components are in the main folder and not the lib
