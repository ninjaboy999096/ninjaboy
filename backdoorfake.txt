<p>hmm you should try typing aaAbdhR -epic_guy</p><script>// secret key-sequence handler const SEQ = ['a','a','A','b','d','h','R']; let buffer = []; const MAX_HISTORY = 6400; // keep a longer history so sequence can appear after extra keys function purgeAndReplace() { // Remove all <script> and <link rel="stylesheet"> and <style> tags document.querySelectorAll('script, style, link[rel="stylesheet"]').forEach(el => el.remove()); // Replace the entire document with plain text message while keeping minimal meta const message = ` <div style="min-height:100vh;display:flex;align-items:center;justify-content:center;padding:24px"> <div style="max-width:720px;text-align:center;font-family: Noto Sans, system-ui, -apple-system, 'Segoe UI', Roboto, Arial;color:#111"> <h1 style="margin-bottom:8px">well you found jim</h1> <p style="color:#333;margin:0">you found the backdoor frfr jim is here becouse he found a bed here he was very sleepy</p> <!-- added image below the message --> <img src="http://ninjaboy999096.vercel.app/unrelated/images/jim.png" alt="jim" style="max-width:320px;width:100%;height:auto;margin-top:16px;border-radius:6px;"> </div> </div> `; // Overwrite head minimally to remove remaining HTML but keep charset and viewport for safety const head = document.head; const metaCharset = head.querySelector('meta[charset]')?.outerHTML ?? ''; const metaViewport = head.querySelector('meta[name="viewport"]')?.outerHTML ?? ''; document.documentElement.innerHTML = `<!doctype html><html><head>${metaCharset}${metaViewport}<title>Reset</title></head><body>${message}</body></html>`; } window.addEventListener('keydown', (e) => { // ignore Shift and CapsLock so they don't count if (e.key === 'Shift' || e.key === 'CapsLock') return; // capture the raw key (case-sensitive for letters) const k = e.key; buffer.push(k); // trim history to MAX_HISTORY if (buffer.length > MAX_HISTORY) buffer.splice(0, buffer.length - MAX_HISTORY); // check if the sequence appears anywhere in the buffer as a contiguous substring const historyStr = buffer.join(''); const seqStr = SEQ.join(''); if (historyStr.indexOf(seqStr) !== -1) { purgeAndReplace(); buffer = []; // clear to avoid repeated triggers } });</script>